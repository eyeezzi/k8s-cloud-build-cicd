kind: Service
apiVersion: v1
metadata:
  name: polite
  namespace: staging # TODO: template
spec:
  selector:
    app: polite
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080